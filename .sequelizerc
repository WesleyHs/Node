const { resolve } = require('path');

module.exports ={
    config: resolver(__dirname, 'src',  'config', 'database.js'), //caminho para a pasta database
    'models-path': resolve(__dirname, 'src', 'app', 'models.js'), //caminho para o models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //caminho para as migrations
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), //caminho para as migrations
};